// ===== バランス調整は全てこのファイルで完結する =====
// 他モジュールにはバランスに効く数値リテラルを置かない。

import type { Rate } from "./types";

export const CONFIG = {
  // ---- 時計（その日の分。12:00=720, 24:00=1440）----
  // 1プレイ ~5分を狙い「夜の繁忙シフト」に短縮。既存 arrivalCurveByHour の
  // ピーク(17-22時=0.6-1.0)をそのまま活かすため 17:00 開店にしてある。
  openMin: 1020, // 17:00 開店（夜シフト・~7.5h営業）
  closeMin: 1410, // 23:30 ラストオーダー（以降は新規来店なし・進行中は完走）
  hardCloseMin: 1470, // 24:30 強制閉店（残っていても締める）
  minutesPerTick: 1, // 1tickでゲーム内1分進む
  advanceTickMs: 80, // ターン制: 自動進行アニメの基準tick間隔（x1）。x2/x4 はこれを割る＝速く。
  maxStepsPerFrame: 10, // 1フレームあたりの最大tick数（停止条件まで滑らかに進める）
  advanceSpeeds: [1, 2, 4] as number[], // 選べる進行速度の倍率（x1=もっともゆっくり）
  urgentRatio: 0.75, // 待ち客の「緊急」しきい値（我慢ゲージ比。停止条件＆赤表示に共用）

  // ---- 半荘の長さ（分）----
  // 局シミュ化に伴い、半荘長は「局数×kyokuMin」で可変（連荘で伸びる）。
  // eastMin/southMin は进行バーや一部表示の目安として残す。
  eastMin: 7, // 東場（目安）
  southMin: 7, // 南場（目安）

  // ---- 局シミュレーション（本格進行: 親番ローテ・局ごとの実点移動）----
  kyoku: {
    kyokuMin: 1.4, // 1局あたりの目安進行分
    maxKyokuPerHanchan: 14, // 半荘の局数上限（連荘暴走ガード）
    maxHonba: 4, // 本場の上限（超えたら親流れ）
    ryuukyokuProb: 0.16, // 流局率
    notenPenalty: 3000, // 流局のノーテン罰符（総額・テンパイ者で山分け）
    tsumoProb: 0.42, // ツモ率（残りはロン）
    winSkillW: 2.2, // 和了者抽選: 強さの効き
    winLuckW: 1.4, // 和了者抽選: ツキの効き
    dealerWinMult: 1.25, // 親は和了しやすい
    // 手の点数分布（点, 重み）。ヘビーテール。大物手の直撃で点棒が大きく振れ、
    // 点5では薄い資金の客が飛びうる（高レートのリスク＝このゲームの肝）。
    handValues: [
      [1500, 26],
      [2900, 23],
      [3900, 18],
      [5200, 12],
      [7700, 9],
      [8000, 7],
      [12000, 4.5],
      [16000, 2.2],
      [24000, 1],
      [32000, 0.5],
    ] as [number, number][],
    // 着席後（半荘前）の待ちは、列で待つより気が長い。我慢ゲージの進みを緩める。
    seatedWaitMult: 0.5,
    // 点5(BLUE)は赤・祝儀の世界で手が大きく振れる。点5卓の手の点数を増幅し、
    // 薄い資金の客が飛ぶ「高レートの怖さ」を演出する（点3=GREENは等倍）。
    // 2.4: 点棒の振れ→素点→円の振れを増やし、薄財布の客を点5で飛ばす（怖さの強化）。
    blueHandMult: 2.4,
  },

  // ---- 卓数 ----
  // 5分で捌ける規模に抑える（レバーB）。CONFIG.maxTables 参照箇所・CSS・staffName
  // フォールバック(maxTables*4)は自動追従する。
  maxTables: 8,

  // ---- 来店 ----
  // 時間帯別の相対重み（昼ピーク・夜ピーク）。キーは「時」。
  arrivalCurveByHour: {
    12: 0.7,
    13: 0.8,
    14: 0.5,
    15: 0.4,
    16: 0.4,
    17: 0.6,
    18: 0.9,
    19: 1.0,
    20: 1.0,
    21: 0.95,
    22: 0.7,
    23: 0.4,
  } as Record<number, number>,
  baseArrivalChancePerTick: 0.085, // 1tickあたりの来店確率の基準値（来店総数↓→決定停止↓・1客の重み↑）
  reputationArrivalFactor: 0.6, // 評判が低いと来店を抑制する強さ（0で無影響）
  maxWaitingSpawn: 10, // 待ち列がこれ以上なら来店抑制（山積みを抑え来店連発を減らす）

  // ---- 希望分布 ----
  prefWeights: { BLUE: 0.25, GREEN: 0.35, ANY: 0.4 } as Record<string, number>,

  // ---- 経済 ----
  // 場代（客1人・1半荘あたり）= プレイヤーの実売上。点5の方を高く設定。
  // 客数を絞った（レバーB）ぶん1客の単価を上げ、利益水準を維持する（少数・高単価）。
  // 点5=¥950: 飛び(churn)のリスクを負うぶん、無事に回せば点3を明確に上回る旨味を持たせる。
  // 「厚財布の客を点5へ、薄財布を点3へ」という采配が利益に効くようにするための主リターン源。
  gameFeeYen: { BLUE: 950, GREEN: 480 } as Record<Rate, number>,
  // レート（1000点あたりの円）。点5=¥110, 点3=¥30。
  // 点5を高くして資金変動を大きくし、薄い資金の客が大敗で飛ぶ「高レートの怖さ」を成立させる。
  rateYenPer1000: { BLUE: 110, GREEN: 30 } as Record<Rate, number>,
  oka: { mochi: 25000, kaeshi: 30000 }, // 25000持ち30000返し → 1位に+20000
  uma: [20, 10, -10, -20] as number[], // ウマ（×1000点）
  // 点棒スプレッドのテンプレ範囲（×1000点）。ゼロサムに正規化される。
  // 振れ幅を大きめにして、負けが込むと資金が尽きる体験を効かせる。
  pointSpread: { xMin: 20, xMax: 48, yMin: 4, yMax: 16 },
  bigSwingChance: 0.14, // 大物手直撃・箱割れの発生率
  bigSwingMult: 1.9, // そのときのトップ/ラスの点差倍率
  // 祝儀（点5のみ）。着順で枚数の期待値が変わる。資金変動を増幅。
  tipYen: 1000,
  tipMeanByRank: [1.2, 0.6, 0.3, 0.1] as number[],

  // ---- 客のステータス分布 ----
  skill: { mean: 0.5, sd: 0.18 },
  luck: { mean: 0.5, sd: 0.15, driftSd: 0.05 },
  bankrollYen: { mean: 28000, sd: 15000, min: 7500 }, // 薄い財布をやや厚めに混ぜ、点5で飛びやすく（怖さ）
  patienceMin: { mean: 28, sd: 9, min: 8 },
  sessionLenMin: { mean: 170, sd: 55, min: 60 }, // leaveByMin = arrival + これ

  // ---- 着順シミュ（economy）----
  settle: { wSkill: 1.0, wLuck: 0.7, sigma: 0.5 },

  // ---- コール / 交代 ----
  moshirasuLeaveProb: 0.4, // モシラスの客が実際に抜ける基準確率
  // ラスハンに寄せるバイアス（帰宅間近・資金薄）。
  callLasthanBias: { nearLeaveByMin: 30, nearLeaveByBoost: 0.45, lowBankrollRatio: 0.3, lowBankrollBoost: 0.35 },
  baseLasthanProb: 0.18, // 平常時にラスハンする確率
  // 客は最低3半荘打つつもりで来店する（硬い保証）。資金が尽きていない限り、
  // 3半荘を打ち終えるまでは自主離席もラスハン離席もしない（2半荘で帰る等は起きない）。
  // 資金切れ（飛び）・帰宅時刻（時間切れ）による離席だけは別経路で従来通り強制される。
  minHanchanIntent: 3, // 客が必ず打つ最低半荘数（資金切れ・時間切れを除く）
  // 交代受諾（多要素・客ごとの条件）。継ぐ席の点棒/トップ差/親残存/自分の待ち時間/性格で判定。
  swap: {
    base: 0.82, // 互角な局頭ではほぼ受諾（不自然な即拒否を解消）
    kPoints: 0.00001, // 継ぐ席の点棒が25000超で受諾↑/下回ると↓
    kTopGap: 0.000018, // トップとの差が大きいほど受諾↓（主要な抑制要因）
    dealerBonus: 0.12, // 継ぐ席が現在の親なら美味しい（親残存）
    kRemain: 0.1, // 半荘の残り（=親番がまだ回る）が多いほど受諾↑
    kWait: 0.25, // 自分の待ち時間が長いほど妥協して受諾↑（拒否した場合の待ち時間）
    kTol: 0.3, // 客ごとの swapTol 性格の効き
    pMin: 0.03,
    pMax: 0.97,
  },
  // 交代許容度 swapTol の生成分布（0..1にクランプ）。
  swapTol: { mean: 0.5, sd: 0.18 },

  // ---- 評判 ----
  reputation: {
    start: 70,
    max: 100,
    min: 0,
    rageHit: 3, // 怒り離席で減
    bustHit: 2.5, // 飛びで減（薄財布を点5に座らせる雑な采配を罰する＝来店減でじわ効く）
    serveGain: 0.6, // 着席させると増
    satisfiedGain: 1.0, // 満足して帰すと増
  },

  // ---- 店員・人件費・利益 ----
  // 店員数は「開店前の設定画面」で一度だけ決める（進行中の増員は不可）。
  staffCount: 2, // 開店前設定の初期値（デフォルト店員数）
  staffMin: 1, // 設定で選べる最小店員数（最大は maxTables*4 を使用箇所で導出＝全席を本走で埋められる上限）
  wagePerHourYen: 1200, // 店員1人あたりの時給（人件費）。多いほど利益を圧迫。
  // スコア評価の基準（利益＝売上−人件費）。
  // 多シード実測（24seed・最適寄りの点5中心プレイ）で利益の中央値が ~¥42k だったため、
  // これを「及第点(B)の中央」に置く。旧50kは“当たりシード”に寄っており、普通に上手い日が
  // C/Dに落ちる校正ズレ（運がDを生む）があった。scoreRank は実測分布に整合した絶対帯で判定する。
  targetProfit: 42000,

  // ---- その他 ----
  logCap: 120,
  targetRevenue: 70000, // 旧基準（参考値・~7.5h営業の目安売上）

  // 客が来店時に既存卓へ案内されるのを待つ猶予など、将来の拡張用フック。
} as const;

/**
 * 評価レター（利益＝売上−人件費 で判定）。
 * 帯は24seed実測（最適寄りの点5中心プレイ）の利益分布に整合した「絶対値」:
 *   分布: 下位の不振日 ~¥6-9k / 本流(B)が ~¥34-51k(中央¥42k) / 上振れ ~¥56-71k。
 *   S≥65k(上位~12%) A≥52k(上位~17%) B≥34k(本流~50%・中央=B) C≥20k(不振) D<20k(惨敗/客足壊滅)。
 * これにより「普通に上手い日=B、攻めて上振れ=A/S、不振日=C、明確な失敗=D」となり、
 * 運の下振れだけでDに落ちにくい（不振日はC止まり）。
 */
export function scoreRank(profit: number): { rank: string; comment: string } {
  if (profit >= 65000) return { rank: "S", comment: "伝説の雀荘マネージャー！点5を攻めて大勝ち。" };
  if (profit >= 52000) return { rank: "A", comment: "見事な卓回し。常連も大満足。" };
  if (profit >= 34000) return { rank: "B", comment: "堅実な営業。及第点。" };
  if (profit >= 20000) return { rank: "C", comment: "客足が鈍い日もある。点5の回転を上げたい。" };
  return { rank: "D", comment: "人件費に見合う回転を作ろう…修行あるのみ。" };
}
